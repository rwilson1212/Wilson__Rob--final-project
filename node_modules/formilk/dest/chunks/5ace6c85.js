import { useEvent, useEffect } from 'atomico';

/**
 *
 * @param {()=>void} fromLabel
 * @param {()=>void} [ignore]
 */
function useEventLabel(fromLabel, ignore) {
  const dispatchPreventLabel = useEvent("PreventLabel", {
    bubbles: true,
    composed: true,
    cancelable: true,
  });

  useEffect(() => {
    dispatchPreventLabel(fromLabel) && ignore && ignore();
  }, []);
}

export { useEventLabel as u };
//# sourceMappingURL=5ace6c85.js.map